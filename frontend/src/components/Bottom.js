// src/Bottom.js
import React from "react";
import styles from "./Bottom.module.css"; // Optional: Create a CSS module for styling

const Bottom = () => {
  return (
    <div className={styles.bottom}>
      {/* You can add content here later */}
      <p>This is the bottom component.</p>
    </div>
  );
};

export default Bottom;
