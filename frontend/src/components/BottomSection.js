import React from "react";
import styles from "./BottomSection.module.css"; // Import the CSS module

const BottomSection = () => {
  return (
    <div className={styles.bottomSectionContainer}>
      <h2>This is the bottom section (empty for now)</h2>
    </div>
  );
};

export default BottomSection;
