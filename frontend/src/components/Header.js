// src/Header.js
import React from "react";
import styles from "./Header.module.css"; // Optional: Create a CSS module for styling

const Header = () => {
  return (
    <header className={styles.header}>
      <h1>Kamala vs Trump Slot Machine</h1>
    </header>
  );
};

export default Header;
